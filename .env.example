# === LLM ===
OPENAI_API_KEY=sk-your-key-here
LLM_MODEL=gpt-4o-mini
LLM_TEMPERATURE=0.7

# === Rate Limiting (prevents "Too many requests" errors) ===
LLM_RPM=10
LLM_MIN_INTERVAL=6.0

# === GitHub Publisher ===
GITHUB_TOKEN=ghp_your-token-here
PUBLISH_REPO_OWNER=dimaedwardsehk
PUBLISH_REPO_NAME=pass-exam-usa
PUBLISH_LAYOUT=flat
PUBLISH_BASE_BRANCH=main

# === Output ===
OUTPUT_DIR=output
